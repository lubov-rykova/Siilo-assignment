# Siilo test project

All supportive documents can be found in [docs](./docs) directory.

To run tests locally:
1. Start Android emulator.
2. Start Appium server.
3. Run `./gradlew test`.

Test reports will be generated in [build/reports/test](./build/reports/test) directory.
Open `index.html` in your browser to view generated report.
